'use strict';const dotenv=require("dotenv");dotenv.config({path:"./config.env"});const appPort=process.env.APP_PORT,DB=process.env.DATABASE_LOCAL,dbPort=process.env.DB_PORT,{app}=require("./app");app.listen(appPort,"127.0.0.1",()=>{console.log(`APPLICATION SERVER IS NOW RUNNING, PORT NUMBER IS ${appPort}`)});const mongoose=require("mongoose");
mongoose.connect(DB,{useNewUrlParser:!0,useCreateIndex:!0,useFindAndModify:!1,useUnifiedTopology:!0}).then(()=>{console.log(`DATABASE SERVER IS NOW RUNNING, PORT NUMBER IS ${dbPort}`)}).catch(a=>console.log(`DATABASE SERVER CONNECTION ERROR ---> ${a}`));